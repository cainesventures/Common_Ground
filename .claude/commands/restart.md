Run the restart script to stop and restart the Common Ground backend and frontend, then open the browser.

Execute this command:
```bash
powershell.exe -ExecutionPolicy Bypass -File "C:\Projects\Common_Ground\restart.ps1"
```
