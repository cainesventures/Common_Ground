Stop the backend and frontend, wipe the database, reseed all debates, run the moderator, then restart everything and open the browser.

Execute this command:
```bash
powershell.exe -ExecutionPolicy Bypass -File "C:\Projects\Common_Ground\fresh_start.ps1"
```
